export enum HTTPStatus {
  SUCCESS = 200,
  ERROR = 400,
  SERVER_ERROR = 500,
}

export enum HTTPMessage {
  SUCCESS = 'Server responded successfully',
  ERROR = 'Server responded error',
  SERVER_ERROR = 'Server error',
}
